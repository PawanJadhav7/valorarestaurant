name: Basic CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  basic-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Basic validation
        run: echo "Basic CI check passed"
